<% if restaurants.present? %>
  <%= paginate restaurants, remote: true %>
  <% restaurants.each do |rst| %>
    <p><%= @likes_count[rst.id]%>人が行きたい！と言っています</p>
    <p>店名：<%= rst.name %>　｜　点数：<%= rst.score%>点</p>
    <p>エリア：<%= rst.area.name %></p>
    <p>住所：<%= rst.address %></p>
    <p>最寄り駅：<%= rst.station %></p>
    <p>電話番号：<%= rst.phone %></p>
    <p>ーーーーーーーーーーーーーーーーーーーーーーーーーー</p>
  <% end %>
<% else %>
  <p>まだ登録されているお店がありません</p>
<% end %>
