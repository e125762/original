<% if restaurants.present? %>
  <h4>ヒット件数：<%= hits %>/<%= total %>件 </h4>
  <% restaurants.each do |restaurant| %>
    <div class="panel panel-default">
      <div class="panel-heading">
        <h4><%= restaurant.name %>　｜　点数：<%= restaurant.score %>点</h4>
      </div>
      <div class="panel-body">
        <p>住所：<%= restaurant.address %></p>
        <p>エリア：<%= restaurant.area.name %></p>
        <p>最寄り駅：<%= restaurant.station %></p>
        <p>電話番号：<%= restaurant.phone %></p>
        <div id="rst_<%= restaurant.id %>">
          <%= render 'likes/like_button', {group_id: params[:group_id], rst: restaurant, group: group} %>
        </div>
      </div>
    </div>
  <% end %>
<%= paginate restaurants, remote: true %>
<% else %>
　　<p>検索結果がありません</p>
<% end %>
